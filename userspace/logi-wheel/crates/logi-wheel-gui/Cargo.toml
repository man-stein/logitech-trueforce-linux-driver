[package]
name = "logi-wheel-gui"
edition = "2021"
version.workspace = true
license = "GPL-3.0-or-later"
rust-version = "1.92"

[dependencies]
slint = "1.17"
logi-wheel-core = { path = "../logi-wheel-core" }
# Only for the Test page's evdev force-feedback ioctls (EVIOCSFF/EVIOCRMFF)
# and the non-blocking open flag; already present in the workspace tree.
libc = "0.2"

[build-dependencies]
slint-build = "1.17"
